// This is a placeholder if you want to add sidebar functionality later
// Currently not used since we're using Navbar for all navigation

const Sidebar = () => {
  return null; // Not used in current layout
};

export default Sidebar;